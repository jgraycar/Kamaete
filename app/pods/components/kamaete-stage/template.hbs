<paper-material elevation=3>
  <svg viewBox="0 0 800 450">
    {{#if instrument}}
      {{kamaete-instrument instrument=instrument}}
    {{else}}
      {{#each instruments as |instrument|}}
        {{kamaete-instrument instrument=instrument}}
      {{/each}}
    {{/if}}
  </svg>
</paper-material>
